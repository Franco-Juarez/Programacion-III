<!DOCTYPE html>
<html lang="es">

<head>
    <title>
        Gestión de Turnos - Clínica
    </title>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header>
        <p>
            Clínica
        </p>
        <button class="button">
            Cerrar sesión
        </button>
    </header>
    <main>
        <h1>
            Sistema de Gestión de Turnos
        </h1>
        <h2>
            Lista de Turnos
        </h2>
        <p>
            Todos los turnos programados con opciones de gestión
        </p>

        <table border="1" cellpadding="8" cellspacing="0">
            <thead>
                <tr>
                    <th>ID Turno</th>
                    <th>Fecha</th>
                    <th>Hora</th>
                    <th>ID Paciente</th>
                </tr>
            </thead>
            <tbody>
                <% if (turnos && turnos.length> 0) { %>
                    <% turnos.forEach(function(turno) { %>
                        <tr>
                            <td>
                                <%= turno.id %>
                            </td>
                            <td>
                                <%= turno.fecha %>
                            </td>
                            <td>
                                <%= turno.hora %>
                            </td>
                            <td>
                                <%= turno.idPaciente %>
                            </td>
                        </tr>
                        <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="4">No hay turnos programados.</td>
                                </tr>
                                <% } %>
            </tbody>
        </table>
        <h2>
            Lista de Pacientes
        </h2>

        <table border="1" cellpadding="8" cellspacing="0">
            <thead>
                <tr>
                    <th>ID Paciente</th>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>DNI</th>
                </tr>
            </thead>
            <tbody>
                <% if (pacientes && pacientes.length> 0) { %>
                    <% pacientes.forEach(function(paciente) { %>
                        <tr>
                            <td>
                                <%= paciente.id %>
                            </td>
                            <td>
                                <%= paciente.nombre %>
                            </td>
                            <td>
                                <%= paciente.apellido %>
                            </td>
                            <td>
                                <%= paciente.dni %>
                            </td>
                        </tr>
                        <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="4">No hay pacientes registrados.</td>
                                </tr>
                                <% } %>
            </tbody>
        </table>
    </main>
    <footer>
        <p>© <%= new Date().getFullYear() %> Mi Aplicación</p>
    </footer>
</body>

</html>